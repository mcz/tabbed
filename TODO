# TODO
* add some way to detach windows
* add some way to attach windows

* icon:
	* support the old way of setting icons via WM_HINTS
	* store icons in a cache so we don't have to recieve a lot of data from
	  the X Server whenever we change tabs.
